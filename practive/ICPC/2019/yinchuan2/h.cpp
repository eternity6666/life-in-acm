#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(0); cin.tie(); cout.tie();
    int n; string s; cin >> n >> s;
    vector<int> w[i];
    for(int i = 1; i <= n; ++i) cin >> w[i];
    
    return 0;
}

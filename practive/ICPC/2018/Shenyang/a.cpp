#include <bits/stdc++.h>
using namespace std;
const int maxn = 100000 + 5;

int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        int n, x, A, B;
        cin >> n >> x >> A >> B;

        bool ans = 1;

        if(ans)
            cout << "Ritsuka" << endl;
        else
            cout << "Gudako" << endl;
    }
}

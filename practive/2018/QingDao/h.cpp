#include <bits/stdc++.h>
using namespace std;

const int maxn = 100000 + 5;
char c[maxn];
int ans = 0;
int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        ans = 0;
        int n = strlen(c);
        for(int p = 0; p <= n - 1; p++)
        {
            for(int q = p + 1; q <= n; q++)
            {

            }
        }
    }
    return 0;
}

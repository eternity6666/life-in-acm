#include <bits/stdc++.h>
using namespace std;

int dp[10][18];
int main()
{
    ios_base::sync_with_stdio(0);
    int t;
    cin >> t;
    while(t--)
    {
        int l, r;
        cin >> l >> r;
        
    }
    return 0;
}

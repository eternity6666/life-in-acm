
            /*
            map<long long, bool>::iterator it = mma.begin();
            fej(1, i - 1)
                it++;
            long long tmp = (*it).first;
            */
            /*
            map<long long, bool>::iterator it = nma.begin();
            fej(1, i - 1)
                it++;
            long long tmp = (*it).first;
            */
    /*
    for(it = nt.begin(); it != nt.end(); it++)
        cout << *it << endl;
    for(it = ns.begin(); it != ns.end(); it++)
        cout << *it << endl;

    for(it = mt.begin(); it != mt.end(); it++)
        cout << *it << endl;
    for(it = ms.begin(); it != ms.end(); it++)
        cout << *it << endl;
        */

#include <iostream>
#include <vector>
#include <set>
#include <map>
#include <algorithm>
#include <cmath>
#include <string>
#include <cstring>
#include <queue>
#include <stack>
#include <fstream>
#include <sstream>
#include <cstdio>
#define usefre

using namespace std;
const int mod = 1000000000 + 7;

long maxd(int n);
long mind(int n);

int main()
{
#ifdef usefre
    freopen("in.txt","r",stdin);
    freopen("out.txt","w",stdout);
#endif

    int n;
    cin >> n;
    long long sum = 0;
    for(;;)
    {
        sum += sumdiff(v);
    }
    cout << sum;
    return 0;
}

long maxd(int n)
{
    long ans;
    return ans;
}

long mind(int n)
{

}

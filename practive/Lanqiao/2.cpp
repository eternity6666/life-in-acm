#include <bits/stdc++.h>
#define fei(a, b) for(int i = a; i <= b; i++)
#define fej(a, b) for(int j = a; j <= b; j++)
#define fek(a, b) for(int k = a; k <= b; k++)
using namespace std;
const double PI = abs(acos(-1));
int main()
{
	// freopen(./in.txt,r, stdin); 
	// freopen(./out.txt, w, stdout);
	
	int n;
	cin >> n; 
	
	double ans = n * n * PI;
	
	printf(%.7lf, ans);
	return 0;
} 

#include <bits/stdc++.h>
using namespace std;

int main()
{
    string s;
    cin >> s;
    /*
    stack<string> st;
    for(int i = 0; i < s.length();)
    {
        if(s[i] <= '9' && s[i] >= '0')
        {
            string tmp = "";
            for(; i < s.length(); i++)
            {
                if(s[i] <= '9' && s[i] >= '0')
                    tmp += s[i];
                else
                    break;
            }
            st.push(tmp);
        }
        else
        {
            st.push(tmp);
            i++;
        }
    }
    */
}

string qToH (string s) {

}



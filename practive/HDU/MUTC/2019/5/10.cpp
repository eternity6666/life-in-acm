#include <iostream>
using namespace std;

int n;
int arr;
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie();
    int t;
    cin >> t;
    while(t--)
    {
        cin >> n;
        cout << n;
    }
    return 0;
}

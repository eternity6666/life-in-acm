#include <bits/stdc++.h>
using namespace std;
int main()
{
    freopen("in.txt", "r", stdin);
    string a, b;
    while(cin >> a)
    {

        if(a == "*")
            break;
        if(a[0] < '0' || a[0] > '9')//words
        {

        }
        else//numbers
        {

        }
    }
    return 0;
}

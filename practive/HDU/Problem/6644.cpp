#include <iostream>
using namespace std;

char a[maxn];

int main()
{
    int t; scanf("%d", &t)
    while(t--)
    {
        int n, m, q; scanf("%d%d%d", &n, &m, &q);
        for(int i = 1; i <= n; i++)
            scanf("%c", &a[i]);
        
    }
}
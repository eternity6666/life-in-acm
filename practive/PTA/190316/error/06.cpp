
    /*
    fei(1, n)
    {
        string s;
        s = getline();
        // x[i][1] = s;
        getchar();
    }
    */
    /*
    fei(1, n)
    {
        // x[i] = getline();
        
        fej(1,n + 1)
        {
            if(j < n)
                x[i][j] = getchar();
            else
                cin >> x[i][j];
        }
        // getchar();
    }
    */

